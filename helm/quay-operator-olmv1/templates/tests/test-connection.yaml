apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quay-operator-olmv1.fullname" . }}-test-connection"
  labels:
    {{- include "quay-operator-olmv1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quay-operator-olmv1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
